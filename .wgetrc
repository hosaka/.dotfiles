# use server last modification date
timestamping = on

# timeout after N seconds (default is 15 min)
timeout = 60

# retry N times (default is 20)
tries = 3

# retry even when connection was refused
retry_connrefused = on

# ignore robots.txt and nofollow meta
robots = off
